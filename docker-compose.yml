version: "3.9"
services:
  worker:
    build: .
    environment:
      APP_ID: $API_ID  # defaults to None
      API_HASH: $API_HASH
      SESSION: $SESSION
      FROM_CHANNEL: $FROM_CHANNEL
      TO_CHANNEL: $TO_CHANNEL
